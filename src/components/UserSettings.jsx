import React from 'react';

const UserSettings = () => {
  return (
    <div className="settings-section">
      <h2 className="section-title">Settings</h2>
      <p className="section-content">Your account settings will appear here.</p>
    </div>
  );
};

export default UserSettings;